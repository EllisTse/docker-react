language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t devopsaron316/docker-react -f Dockerfile.dev .

  script:
  - docker run -e CI=true devopsaron316/docker-react npm run test
